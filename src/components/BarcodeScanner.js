import React from 'react';

const BarcodeScanner = () => {
    return (
        <div>
            <h2>Barcode Scanner</h2>
            <p>Scan the barcode of a product to get its nutritional information.</p>
        </div>
    );
};

export default BarcodeScanner;